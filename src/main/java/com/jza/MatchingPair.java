package com.jza;

public class MatchingPair {


	/**
	 * Method checks if there are 2 numbers that summed up are equal to expected sum.
	 * Can't add the same number to itself.
	 * Numbers in input array are sorted ascending.
	 * Values in array can be negative.
	 *
	 * @param numbers Array of numbers.
	 * @param sum     Required sum of 2 numbers.
	 * @return boolean: true if pair exists, false if pair doesn't exist.
	 */
	boolean containsMatchingPair(int[] numbers, int sum) {
		return false;
	}
}
